import 'package:flutter/material.dart';

class AddIngredientsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Add Ingredients Page Content',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}